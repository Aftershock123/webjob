<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .profile-img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 10px;
    }

    .upload-btn {
      position: absolute;
      margin-left: 50%;
      padding-top: 20%;
      top: 10px;
      right: 10px;
      background-color: rgba(0, 0, 0, 0.5);
      color: white;
      border: none;
      border-radius: 50%;
      padding: 10px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .form-control-input {
      margin-bottom: 10px;
    }
  </style>
</head>

<body>

  <%- include('navbar') %>


    <section style="background-color:transparent; height: 100%;">
      <form action="/user/updateprofile/<%= user.id_user %>" method="post" enctype="multipart/form-data">
        <div class="container d-flex justify-content-center py-5 h-100" style="gap: 1rem;background-color: transparent; ">
          <div class="card text-center d-flex justify-content-center align-items-center  text-white background "
            style="width: 300px;">
            <label for="image" class="upload-btn"><i class="fas fa-camera fa-xs"></i></label>
            <img id="imagePreview" src="/image/<%= user.image %>" alt="Avatar" class="img-fluid profile-img "
              style="background-color: #eeeeee;" />
            <input type="file" id="image" name="image" accept="image/*" onchange="displayImage(event)" class="d-none">
          </div>
          <div class="card p-4 " style="width: 500px;">
            <p class="d-flex justify-content-center"><b>ข้อมูลผู้ใช้</b></p>
            <hr class="mt-0 mb-4">
            <input type="text" id="username" name="username" class="form-control mb-2"
              value="<%= user.username %>" />
            <input type="email" id="email" name="email" class="form-control mb-4"
              value="<%= user.email %>" />
            <button type="submit" class="btn w-100" style="color: #eeeeee ; background-color: #ED2A30;">ยืนยัน</button>
          </div>
        </div>
      </form>

      <% if (history=='undefined' || history==null || resume==='undefined' || resume===null) { %>

      <%}else {%>
          
           <%- include('jobsendcard') %>     
      <% } %>
                  
    </section>
    <script>
      function displayImage(event) {
        const image = document.getElementById('imagePreview');
        image.src = URL.createObjectURL(event.target.files[0]);
      }
    </script>
    </div>

</body>

</html>